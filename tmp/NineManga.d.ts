import { Chapter, ChapterDetails, HomeSection, PagedResults, SearchRequest, SourceManga, TagSection, Request, Response, SearchResultsProviding, MangaProviding, ChapterProviding, HomePageSectionsProviding } from '@paperback/types';
export declare const getExportVersion: (EXTENSION_VERSION: string) => string;
export declare abstract class NineManga implements SearchResultsProviding, MangaProviding, ChapterProviding, HomePageSectionsProviding {
    private cheerio;
    constructor(cheerio: any);
    userAgentRandomizer: string;
    requestManager: import("@paperback/types").RequestManager;
    abstract baseUrl: string;
    abstract languageCode: string;
    abstract genreTag: string;
    abstract authorTag: string;
    abstract statusTag: string;
    alternativeChapterUrl: boolean;
    parser: any;
    RETRIES: number;
    getMangaShareUrl(mangaId: string): string;
    supportsTagExclusion(): Promise<boolean>;
    getMangaDetails(mangaId: string): Promise<SourceManga>;
    getChapters(mangaId: string): Promise<Chapter[]>;
    getChapterDetails(mangaId: string, chapterId: string): Promise<ChapterDetails>;
    getSearchResults(query: SearchRequest, metadata: any): Promise<PagedResults>;
    getTags(): Promise<TagSection[]>;
    getHomePageSections(sectionCallback: (section: HomeSection) => void): Promise<void>;
    getViewMoreItems(_: string, __: any): Promise<PagedResults>;
    protected convertTime(timeAgo: string): Date;
    parseStatus(str: string): string;
    createRequest(url: string): any;
    constructSearchRequest(page: number, query: SearchRequest): any;
    constructHeaders(headers?: any, refererPath?: string): any;
    getCloudflareBypassRequest(): Promise<Request>;
    checkResponseError(response: Response): void;
}
